const fileSys = require('fs')

// read content.

// fileSys.readFile('./docs/blog1.txt', async (error, data) => {
//     await error ? console.log(error) : console.log(data.toString()) ;
// })

// writing files

// fileSys.writeFile('./docs/blog.txt', 'plus theme, I am a programer', (error) => {
//     error ? console.log('face an error') : console.log('text added.');
// })

// append to file

// fileSys.appendFile('./docs/blog.txt', '\nplus theme, I am a programer', (error) => {
//     error ? console.log('face an error') : console.log('text added.');
// })

// directory

// fileSys.mkdir('./mamadoo', () => console.log('folder clreated'))

// remove directory (for folder)

// fileSys.rmdir('./mamadoo', (error) => (!error) ? console.log('file removed') : console.log('faace an error.') )

// remove directory (for file)

// fileSys.unlink('./docs/majid.txt', (error) => (!error) ? console.log('file removed') : console.log('faace an error.') )


